# 客服在线开关 + 派单（替代排班）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `staff_shifts` 排班模型替换为 `staff_presence` 在线开关 + 服务端"最少在聊"派单（60s 超时回退池），删 shifts 全套。

**Architecture:** 复用 `staff_presence` 承载 online/offline；新增 `services/dispatch.py` 提供 `dispatch_to_human_pending` 单入口 + 后台 watcher；conversation 状态机不变，`mode='human_pending' AND assigned_staff_id IS NOT NULL` = "派给某人未接管"。RBAC 新加 `chat.dispatch` 权限项过滤派单池角色。

**Tech Stack:** Python 3.12 / FastAPI / databases(SQLAlchemy core) / asyncpg / SQLite(测试) / pytest / React 18 / antd / Vite / vitest

**Spec:** `docs/superpowers/specs/2026-06-04-客服在线开关与派单-design.md`

---

## Phase 1 — 后端基础设施（不破坏现有功能）

### Task 1: RBAC 加 chat.dispatch 权限项

**Files:**
- Modify: `server/src/ai_engine/persistence/rbac.py:10-28`
- Test: `server/tests/persistence/test_rbac.py`（若不存在则 Create）

- [ ] **Step 1: 找到现有 rbac 测试或新建**

Run: `ls server/tests/persistence/test_rbac.py 2>/dev/null || echo "missing"`

如果 missing，新建 `server/tests/persistence/test_rbac.py`：

```python
import pytest

from ai_engine.persistence import rbac


@pytest.mark.asyncio
async def test_chat_dispatch_default_matrix(_clean_db):
    rbac.invalidate_cache()
    assert await rbac.is_permitted("agent", "chat.dispatch") is True
    assert await rbac.is_permitted("senior", "chat.dispatch") is True
    assert await rbac.is_permitted("supervisor", "chat.dispatch") is True
    assert await rbac.is_permitted("engineer", "chat.dispatch") is False
    assert await rbac.is_permitted("manager", "chat.dispatch") is False
    assert await rbac.is_permitted("admin", "chat.dispatch") is True


@pytest.mark.asyncio
async def test_chat_dispatch_in_permission_keys():
    assert "chat.dispatch" in rbac.PERMISSION_KEYS
    assert "admin.shifts" not in rbac.PERMISSION_KEYS  # 同时确认旧 key 已删
```

如果文件已存在，把上面两个 test 追加到末尾。

- [ ] **Step 2: 运行测试确认 FAIL**

Run: `cd server && uv run pytest tests/persistence/test_rbac.py -k chat_dispatch -v`
Expected: FAIL with `chat.dispatch not in PERMISSION_KEYS` 或 `admin.shifts in PERMISSION_KEYS`

- [ ] **Step 3: 修 rbac.py**

修改 `server/src/ai_engine/persistence/rbac.py`：

```python
# line 10-17 改为
PERMISSION_KEYS: list[str] = [
    "admin.dashboard",
    "admin.staff",
    "admin.sla",
    "admin.rbac",
    "admin.presence",
    "chat.dispatch",
]

# line 19-28 改为
_DEFAULT_MATRIX: dict[str, set[str]] = {
    "agent": {"chat.dispatch"},
    "senior": {"chat.dispatch"},
    "supervisor": {
        "admin.dashboard", "admin.sla", "admin.presence",
        "chat.dispatch",
    },
    "engineer": set(),
    "manager": {"admin.dashboard"},
    "admin": set(PERMISSION_KEYS),
}
```

- [ ] **Step 4: 运行测试确认 PASS**

Run: `cd server && uv run pytest tests/persistence/test_rbac.py -k chat_dispatch -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add server/src/ai_engine/persistence/rbac.py server/tests/persistence/test_rbac.py
git commit -m "feat(rbac): 加 chat.dispatch 权限项 + 默认值（替排班 admin.shifts）"
```

---

### Task 2: staff_presence persistence 加 release_pending_assignments

**Files:**
- Modify: `server/src/ai_engine/persistence/staff_presence.py`
- Test: `server/tests/persistence/test_staff_presence.py`（若不存在则 Create）

- [ ] **Step 1: 写测试 release_pending_assignments**

新建/追加 `server/tests/persistence/test_staff_presence.py`：

```python
import pytest

from ai_engine.persistence import conversations as conv_dao
from ai_engine.persistence import db, staff_presence
from ai_engine.persistence.schema import now_str


@pytest.mark.asyncio
async def test_release_pending_assignments_releases_pending_only(_clean_db):
    # 准备：一个 staff 有两个被派会话 + 一个已接管
    cid_pending_1 = await conv_dao.create_conversation(user_type="c", subject_id="u1")
    cid_pending_2 = await conv_dao.create_conversation(user_type="c", subject_id="u2")
    cid_takeover = await conv_dao.create_conversation(user_type="c", subject_id="u3")
    sid = "staff-A"

    await db.execute(
        "UPDATE conversations SET mode='human_pending', assigned_staff_id=:s, "
        "assigned_at=:t WHERE id IN (:a, :b)",
        {"s": sid, "t": now_str(), "a": cid_pending_1, "b": cid_pending_2},
    )
    await db.execute(
        "UPDATE conversations SET mode='human_takeover', assigned_staff_id=:s, "
        "assigned_at=:t WHERE id=:c",
        {"s": sid, "t": now_str(), "c": cid_takeover},
    )

    released = await staff_presence.release_pending_assignments(sid)

    assert sorted(released) == sorted([cid_pending_1, cid_pending_2])
    # human_takeover 那条不应被动
    row = await db.fetch_one(
        "SELECT mode, assigned_staff_id FROM conversations WHERE id=:c",
        {"c": cid_takeover},
    )
    assert row["mode"] == "human_takeover"
    assert row["assigned_staff_id"] == sid


@pytest.mark.asyncio
async def test_release_pending_assignments_empty_returns_empty_list(_clean_db):
    assert await staff_presence.release_pending_assignments("nobody") == []
```

- [ ] **Step 2: 运行测试确认 FAIL**

Run: `cd server && uv run pytest tests/persistence/test_staff_presence.py -v`
Expected: FAIL with `AttributeError: module 'staff_presence' has no attribute 'release_pending_assignments'`

- [ ] **Step 3: 实现 release_pending_assignments**

在 `server/src/ai_engine/persistence/staff_presence.py` 末尾追加：

```python
async def release_pending_assignments(staff_id: str) -> list[int]:
    """关在线开关时释放：把当前 mode=human_pending 且派给该 staff 的会话清回开放池。
    已 human_takeover 的会话不动（允许聊完）。返回被释放的 conv_id 列表，给调用方推 SSE 用。

    SQLite 不支持 UPDATE...RETURNING（3.35 之前），用 SELECT→UPDATE 两步走，跨 PG/SQLite 都稳。
    并发安全：两步之间另一线程可能新派给该 staff，那次新派会在下一次开关切换时被收掉，
    不需要事务包裹（worst case 是一次派单短暂悬空，可接受）。
    """
    rows = await db.fetch_all(
        "SELECT id FROM conversations "
        "WHERE assigned_staff_id=:sid AND mode='human_pending'",
        {"sid": staff_id},
    )
    ids = [int(r["id"]) for r in rows]
    if not ids:
        return []
    # databases 库不允许 IN (:list)，按数量动态拼占位符
    placeholders = ",".join(f":id{i}" for i in range(len(ids)))
    params = {f"id{i}": v for i, v in enumerate(ids)}
    params["sid"] = staff_id
    await db.execute(
        f"UPDATE conversations SET assigned_staff_id=NULL, assigned_at=NULL "
        f"WHERE id IN ({placeholders}) AND assigned_staff_id=:sid AND mode='human_pending'",
        params,
    )
    return ids
```

- [ ] **Step 4: 运行测试确认 PASS**

Run: `cd server && uv run pytest tests/persistence/test_staff_presence.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add server/src/ai_engine/persistence/staff_presence.py server/tests/persistence/test_staff_presence.py
git commit -m "feat(presence): 加 release_pending_assignments（关开关时释放未接管派单）"
```

---

### Task 3: staff_presence API heartbeat 端点改造（offline 副作用 + status 收敛）

**Files:**
- Modify: `server/src/ai_engine/api/staff_presence.py:16`（_VALID_STATUS）
- Modify: `server/src/ai_engine/api/staff_presence.py:23-37`（heartbeat 端点）
- Test: `server/tests/api/test_staff_presence_api.py`（若不存在则 Create）

- [ ] **Step 1: 写测试**

新建/追加 `server/tests/api/test_staff_presence_api.py`：

```python
import pytest
from httpx import AsyncClient

from ai_engine.persistence import conversations as conv_dao
from ai_engine.persistence import db
from ai_engine.persistence.schema import now_str


@pytest.mark.asyncio
async def test_presence_offline_releases_pending_returns_count(
    client: AsyncClient, _clean_db, _agent_token
):
    # 准备：当前 staff（agent）被派了一个 pending
    sid = "agent-1"
    cid = await conv_dao.create_conversation(user_type="c", subject_id="u1")
    await db.execute(
        "UPDATE conversations SET mode='human_pending', assigned_staff_id=:s, "
        "assigned_at=:t WHERE id=:c",
        {"s": sid, "t": now_str(), "c": cid},
    )

    r = await client.post(
        "/staff/api/v1/presence",
        json={"status": "offline"},
        headers={"Authorization": f"Bearer {_agent_token(sid)}"},
    )
    assert r.status_code == 200
    body = r.json()
    assert body["ok"] is True
    assert body["released_count"] == 1


@pytest.mark.asyncio
async def test_presence_online_does_not_release(
    client: AsyncClient, _clean_db, _agent_token
):
    sid = "agent-2"
    cid = await conv_dao.create_conversation(user_type="c", subject_id="u1")
    await db.execute(
        "UPDATE conversations SET mode='human_pending', assigned_staff_id=:s, "
        "assigned_at=:t WHERE id=:c",
        {"s": sid, "t": now_str(), "c": cid},
    )

    r = await client.post(
        "/staff/api/v1/presence",
        json={"status": "online"},
        headers={"Authorization": f"Bearer {_agent_token(sid)}"},
    )
    assert r.status_code == 200
    assert r.json()["released_count"] == 0
    # 派单仍在
    row = await db.fetch_one("SELECT assigned_staff_id FROM conversations WHERE id=:c", {"c": cid})
    assert row["assigned_staff_id"] == sid


@pytest.mark.asyncio
async def test_presence_away_collapsed_to_online(client: AsyncClient, _clean_db, _agent_token):
    """status 收敛：away 不再是合法值，应静默回退到 online（与原 invalid 兜底一致）"""
    r = await client.post(
        "/staff/api/v1/presence",
        json={"status": "away"},
        headers={"Authorization": f"Bearer {_agent_token('agent-3')}"},
    )
    assert r.status_code == 200
    # 库里应该是 online（away 不在 _VALID_STATUS）
    row = await db.fetch_one(
        "SELECT status FROM staff_presence WHERE staff_id='agent-3'"
    )
    assert row["status"] == "online"
```

> 注意：`_agent_token` / `client` / `_clean_db` 是项目里已有的 conftest fixture。若实际命名不同，照搬同目录其他 api 测试的 fixture 名。

- [ ] **Step 2: 运行测试确认 FAIL**

Run: `cd server && uv run pytest tests/api/test_staff_presence_api.py -v`
Expected: FAIL（at minimum `released_count` 字段缺失）

- [ ] **Step 3: 改 _VALID_STATUS + heartbeat 端点**

修改 `server/src/ai_engine/api/staff_presence.py`：

```python
# line 16 改为
_VALID_STATUS = {"online", "offline"}

# line 23-37 整段替换为
@router.post("/staff/api/v1/presence")
async def heartbeat(
    body: HeartbeatIn, staff: dict[str, Any] = Depends(require_staff)
) -> dict[str, Any]:
    # 非白名单 status 静默回退 online；away 从此被并入此路径（去掉 away 状态）
    if body.status not in _VALID_STATUS:
        logger.debug("invalid presence status %r, defaulting to online", body.status)
        status = "online"
    else:
        status = body.status
    staff_id = str(staff.get("sub", ""))
    if not staff_id:
        return {"ok": False}
    await staff_presence.heartbeat(staff_id, status)
    released_count = 0
    if status == "offline":
        from ai_engine.api.staff_conversations import publish_conversation_event
        released_ids = await staff_presence.release_pending_assignments(staff_id)
        for cid in released_ids:
            publish_conversation_event(cid, {"type": "conversation_assignment_cleared"})
        released_count = len(released_ids)
    return {"ok": True, "released_count": released_count}
```

- [ ] **Step 4: 运行测试确认 PASS**

Run: `cd server && uv run pytest tests/api/test_staff_presence_api.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add server/src/ai_engine/api/staff_presence.py server/tests/api/test_staff_presence_api.py
git commit -m "feat(presence): heartbeat 端点支持 offline 副作用 + status 收敛 online/offline"
```

---

### Task 4: 写 services/dispatch.py — dispatch_to_human_pending（不含 watcher）

**Files:**
- Create: `server/src/ai_engine/services/__init__.py`（若不存在）
- Create: `server/src/ai_engine/services/dispatch.py`
- Create: `server/tests/services/__init__.py`（若不存在）
- Create: `server/tests/services/test_dispatch.py`

- [ ] **Step 1: 写测试 dispatch_to_human_pending**

```python
# server/tests/services/test_dispatch.py
import pytest

from ai_engine.persistence import conversations as conv_dao
from ai_engine.persistence import db, staff_presence
from ai_engine.persistence.schema import now_str
from ai_engine.services import dispatch


async def _seed_staff(staff_id: str, role: str = "agent"):
    """直接写 staff 表 + presence 表 + 心跳"""
    await db.execute(
        "INSERT INTO staff(id, role, name, password_hash) "
        "VALUES (:i, :r, :n, '') ON CONFLICT(id) DO UPDATE SET role=:r",
        {"i": staff_id, "r": role, "n": staff_id},
    )
    await staff_presence.heartbeat(staff_id, "online")


@pytest.mark.asyncio
async def test_dispatch_picks_least_loaded(_clean_db):
    # A:2 在聊  B:0  C:1 → 应选 B
    await _seed_staff("A")
    await _seed_staff("B")
    await _seed_staff("C")
    for sid, loads in [("A", 2), ("C", 1)]:
        for _ in range(loads):
            cid = await conv_dao.create_conversation(user_type="c", subject_id="x")
            await db.execute(
                "UPDATE conversations SET mode='human_takeover', "
                "assigned_staff_id=:s, assigned_at=:t WHERE id=:c",
                {"s": sid, "t": now_str(), "c": cid},
            )

    new_cid = await conv_dao.create_conversation(user_type="c", subject_id="user-x")
    await conv_dao.set_mode(new_cid, "human_pending")

    result = await dispatch.dispatch_to_human_pending(new_cid)
    assert result["dispatched"] is True
    assert result["staff_id"] == "B"

    row = await db.fetch_one(
        "SELECT assigned_staff_id FROM conversations WHERE id=:c", {"c": new_cid}
    )
    assert row["assigned_staff_id"] == "B"


@pytest.mark.asyncio
async def test_dispatch_no_one_online_returns_flag(_clean_db):
    cid = await conv_dao.create_conversation(user_type="c", subject_id="u")
    await conv_dao.set_mode(cid, "human_pending")
    result = await dispatch.dispatch_to_human_pending(cid)
    assert result == {"dispatched": False, "no_one_online": True}


@pytest.mark.asyncio
async def test_dispatch_excludes_role_without_chat_dispatch(_clean_db):
    # engineer 角色没 chat.dispatch 权限，不应被选
    await _seed_staff("eng", role="engineer")
    cid = await conv_dao.create_conversation(user_type="c", subject_id="u")
    await conv_dao.set_mode(cid, "human_pending")
    result = await dispatch.dispatch_to_human_pending(cid)
    assert result["dispatched"] is False
    assert result["no_one_online"] is True


@pytest.mark.asyncio
async def test_dispatch_excludes_stale_heartbeat(_clean_db):
    # 心跳 6 分钟前 → 超出 5min 窗口，不可派
    await _seed_staff("zombie")
    await db.execute(
        "UPDATE staff_presence SET last_seen_at=:t WHERE staff_id='zombie'",
        {"t": "2026-01-01 00:00:00"},
    )
    cid = await conv_dao.create_conversation(user_type="c", subject_id="u")
    await conv_dao.set_mode(cid, "human_pending")
    result = await dispatch.dispatch_to_human_pending(cid)
    assert result["dispatched"] is False
    assert result["no_one_online"] is True


@pytest.mark.asyncio
async def test_dispatch_idempotent_when_already_assigned(_clean_db):
    """已经派给某人后再次调 dispatch，不应覆盖。"""
    await _seed_staff("A")
    cid = await conv_dao.create_conversation(user_type="c", subject_id="u")
    await conv_dao.set_mode(cid, "human_pending")
    await db.execute(
        "UPDATE conversations SET assigned_staff_id='already', assigned_at=:t WHERE id=:c",
        {"t": now_str(), "c": cid},
    )

    result = await dispatch.dispatch_to_human_pending(cid)
    assert result["dispatched"] is False
    assert result.get("already_assigned") is True
    row = await db.fetch_one("SELECT assigned_staff_id FROM conversations WHERE id=:c", {"c": cid})
    assert row["assigned_staff_id"] == "already"
```

- [ ] **Step 2: 运行测试确认 FAIL**

Run: `cd server && uv run pytest tests/services/test_dispatch.py -v`
Expected: FAIL with `ModuleNotFoundError: ai_engine.services`

- [ ] **Step 3: 创建 services 包 + dispatch.py**

新建 `server/src/ai_engine/services/__init__.py`（空文件）

新建 `server/src/ai_engine/services/dispatch.py`：

```python
"""转人工派单 service：服务端"最少在聊"派单 + 60s 超时回退池。

替代原 shifts_query 班外判断模型。两个调用入口：
  1. api/user_events.py::request_human（C 端用户主动转人工）
  2. agent/tools/create_ticket.py::_ensure_human_pending（AI 工具触发）

派给某人后客服侧 SSE 推 conversation_assigned；60s 内未 take 则后台 watcher
清空 assigned_staff_id → 推 conversation_assignment_cleared 让所有 online 客服可抢。
"""

import logging
from datetime import UTC, datetime, timedelta

from ai_engine.persistence import db, rbac
from ai_engine.persistence.schema import now_str

logger = logging.getLogger(__name__)

# 心跳活跃窗口：与 admin presence 列表的 600s 不完全一致，派单池更严格（5 分钟）
_HEARTBEAT_WINDOW_SECONDS = 300


async def _roles_with_chat_dispatch() -> list[str]:
    matrix = await rbac.list_matrix()
    return [role for role, perms in matrix.items() if perms.get("chat.dispatch")]


async def _pick_least_loaded(roles: list[str]) -> str | None:
    if not roles:
        return None
    cutoff = (datetime.now(UTC) - timedelta(seconds=_HEARTBEAT_WINDOW_SECONDS)).strftime(
        "%Y-%m-%d %H:%M:%S"
    )
    # databases 不支持 IN (:list)，手工拼占位符
    role_placeholders = ",".join(f":r{i}" for i in range(len(roles)))
    params: dict[str, object] = {"cutoff": cutoff}
    for i, r in enumerate(roles):
        params[f"r{i}"] = r

    row = await db.fetch_one(
        f"""
        SELECT p.staff_id,
               (SELECT COUNT(*) FROM conversations c
                WHERE c.mode='human_takeover' AND c.assigned_staff_id=p.staff_id) AS load
          FROM staff_presence p
          JOIN staff s ON s.id = p.staff_id
         WHERE p.status='online'
           AND p.last_seen_at >= :cutoff
           AND s.role IN ({role_placeholders})
         ORDER BY load ASC, p.last_seen_at ASC
         LIMIT 1
        """,
        params,
    )
    return str(row["staff_id"]) if row else None


async def dispatch_to_human_pending(conv_id: int) -> dict:
    """转人工时按"最少在聊"派单。
    返回:
      {dispatched: True, staff_id: X, no_one_online: False}      —— 成功派给某人
      {dispatched: False, no_one_online: True}                   —— 全员 offline / 全无权限
      {dispatched: False, already_assigned: True}                —— 并发：上一次调用已派
    """
    roles = await _roles_with_chat_dispatch()
    picked = await _pick_least_loaded(roles)
    if not picked:
        return {"dispatched": False, "no_one_online": True}

    # 条件更新：仅当 mode='human_pending' AND assigned_staff_id IS NULL 时落盘
    rowcount = await db.execute(
        "UPDATE conversations SET assigned_staff_id=:sid, assigned_at=:now "
        "WHERE id=:id AND mode='human_pending' AND assigned_staff_id IS NULL",
        {"sid": picked, "now": now_str(), "id": conv_id},
    )
    # databases 库 execute 返回行数依驱动而异；用 SELECT 复查更稳
    row = await db.fetch_one(
        "SELECT assigned_staff_id FROM conversations WHERE id=:id", {"id": conv_id}
    )
    if not row or str(row["assigned_staff_id"] or "") != picked:
        return {"dispatched": False, "already_assigned": True}

    # SSE 推到该客服侧（晚 import 避免循环）
    from ai_engine.api.staff_conversations import publish_conversation_event
    publish_conversation_event(conv_id, {"type": "conversation_assigned", "staff_id": picked})

    return {"dispatched": True, "staff_id": picked, "no_one_online": False}
```

- [ ] **Step 4: 运行测试确认 PASS**

Run: `cd server && uv run pytest tests/services/test_dispatch.py -v`
Expected: PASS（5 测试全过）

如有失败先看是 staff 表 schema（看 `persistence/staff.py` 实际字段名）/ fixture 名不对，对照修。

- [ ] **Step 5: Commit**

```bash
git add server/src/ai_engine/services/ server/tests/services/
git commit -m "feat(dispatch): 加 dispatch_to_human_pending（最少在聊 + RBAC 过滤 + 心跳窗口）"
```

---

### Task 5: dispatch watcher（60s 超时回退）+ lifespan 接入

**Files:**
- Modify: `server/src/ai_engine/services/dispatch.py`
- Modify: `server/src/ai_engine/main.py`
- Test: `server/tests/services/test_dispatch_watcher.py`

- [ ] **Step 1: 写测试 watcher**

新建 `server/tests/services/test_dispatch_watcher.py`：

```python
import pytest

from ai_engine.persistence import conversations as conv_dao
from ai_engine.persistence import db
from ai_engine.services import dispatch


@pytest.mark.asyncio
async def test_watcher_clears_timed_out_assignments(_clean_db):
    # 模拟一个 60s 前派出的 pending → watcher 应清空
    cid = await conv_dao.create_conversation(user_type="c", subject_id="u")
    await conv_dao.set_mode(cid, "human_pending")
    await db.execute(
        "UPDATE conversations SET assigned_staff_id='stale', assigned_at='2026-01-01 00:00:00' "
        "WHERE id=:c",
        {"c": cid},
    )

    released_ids = await dispatch._scan_and_clear_timeouts()
    assert cid in released_ids
    row = await db.fetch_one(
        "SELECT assigned_staff_id, assigned_at FROM conversations WHERE id=:c", {"c": cid}
    )
    assert row["assigned_staff_id"] is None
    assert row["assigned_at"] is None


@pytest.mark.asyncio
async def test_watcher_keeps_recent_assignments(_clean_db):
    from ai_engine.persistence.schema import now_str
    cid = await conv_dao.create_conversation(user_type="c", subject_id="u")
    await conv_dao.set_mode(cid, "human_pending")
    await db.execute(
        "UPDATE conversations SET assigned_staff_id='fresh', assigned_at=:t WHERE id=:c",
        {"t": now_str(), "c": cid},
    )

    released = await dispatch._scan_and_clear_timeouts()
    assert cid not in released


@pytest.mark.asyncio
async def test_watcher_ignores_human_takeover(_clean_db):
    """已 takeover 的不应被 watcher 误清"""
    cid = await conv_dao.create_conversation(user_type="c", subject_id="u")
    await db.execute(
        "UPDATE conversations SET mode='human_takeover', "
        "assigned_staff_id='X', assigned_at='2026-01-01 00:00:00' WHERE id=:c",
        {"c": cid},
    )
    released = await dispatch._scan_and_clear_timeouts()
    assert cid not in released
    row = await db.fetch_one("SELECT assigned_staff_id FROM conversations WHERE id=:c", {"c": cid})
    assert row["assigned_staff_id"] == "X"
```

- [ ] **Step 2: 运行测试确认 FAIL**

Run: `cd server && uv run pytest tests/services/test_dispatch_watcher.py -v`
Expected: FAIL with `AttributeError: dispatch has no attribute '_scan_and_clear_timeouts'`

- [ ] **Step 3: 加 watcher 实现**

在 `server/src/ai_engine/services/dispatch.py` 末尾追加：

```python
import asyncio

_TIMEOUT_SECONDS = 60
_WATCHER_INTERVAL_SECONDS = 10


async def _scan_and_clear_timeouts() -> list[int]:
    """扫一遍超时的 human_pending 派单，清回开放池。返回被清空的 conv_id 列表。"""
    cutoff = (datetime.now(UTC) - timedelta(seconds=_TIMEOUT_SECONDS)).strftime(
        "%Y-%m-%d %H:%M:%S"
    )
    rows = await db.fetch_all(
        "SELECT id FROM conversations "
        "WHERE mode='human_pending' "
        "  AND assigned_staff_id IS NOT NULL "
        "  AND assigned_at < :cutoff",
        {"cutoff": cutoff},
    )
    ids = [int(r["id"]) for r in rows]
    if not ids:
        return []
    placeholders = ",".join(f":id{i}" for i in range(len(ids)))
    params: dict[str, object] = {"cutoff": cutoff}
    for i, v in enumerate(ids):
        params[f"id{i}"] = v
    await db.execute(
        f"UPDATE conversations SET assigned_staff_id=NULL, assigned_at=NULL "
        f"WHERE id IN ({placeholders}) "
        f"  AND mode='human_pending' "
        f"  AND assigned_at < :cutoff",
        params,
    )
    return ids


async def _watcher_loop() -> None:
    """常驻协程：每 10 秒扫一遍，超时派单回开放池 + SSE 推所有相关订阅者。"""
    from ai_engine.api.staff_conversations import publish_conversation_event

    while True:
        try:
            cleared = await _scan_and_clear_timeouts()
            for cid in cleared:
                publish_conversation_event(cid, {"type": "conversation_assignment_cleared"})
        except Exception:
            logger.exception("dispatch watcher iteration failed")
        await asyncio.sleep(_WATCHER_INTERVAL_SECONDS)


def start_watcher() -> asyncio.Task:
    """在 FastAPI lifespan startup 中调一次。返回 Task 便于关闭时 cancel。"""
    return asyncio.create_task(_watcher_loop(), name="dispatch_watcher")
```

- [ ] **Step 4: 运行测试确认 PASS**

Run: `cd server && uv run pytest tests/services/test_dispatch_watcher.py -v`
Expected: PASS

- [ ] **Step 5: 接入 main.py lifespan**

打开 `server/src/ai_engine/main.py`，找到现有 `lifespan` 或 startup 钩子，添加：

```python
# 在 lifespan startup 段加：
from ai_engine.services.dispatch import start_watcher
dispatch_task = start_watcher()

# 在 lifespan shutdown 段加：
dispatch_task.cancel()
try:
    await dispatch_task
except asyncio.CancelledError:
    pass
```

（若 main.py 没有 lifespan，看现有 `@app.on_event("startup")` 装饰器，按同模式加）

- [ ] **Step 6: Commit**

```bash
git add server/src/ai_engine/services/dispatch.py server/src/ai_engine/main.py server/tests/services/test_dispatch_watcher.py
git commit -m "feat(dispatch): watcher 每 10s 扫超时派单回开放池 + lifespan 接入"
```

---

## Phase 2 — 切换调用方（接入 dispatch）

### Task 6: create_ticket._ensure_human_pending 改用 dispatch

**Files:**
- Modify: `server/src/ai_engine/agent/tools/create_ticket.py:255-284`
- Test: `server/tests/agent/test_create_ticket_dispatch.py`（若不存在则 Create）

- [ ] **Step 1: 写测试**

```python
# server/tests/agent/test_create_ticket_dispatch.py
import pytest

from ai_engine.agent.tools import create_ticket
from ai_engine.persistence import conversations as conv_dao
from ai_engine.persistence import db


@pytest.mark.asyncio
async def test_ensure_human_pending_no_one_online(_clean_db):
    cid = await conv_dao.create_conversation(user_type="c", subject_id="u")
    info = await create_ticket._ensure_human_pending(cid)
    assert info == {"no_one_online": True}
    # mode 应已切到 human_pending
    mode, _ = await conv_dao.get_mode(cid)
    assert mode == "human_pending"


@pytest.mark.asyncio
async def test_ensure_human_pending_dispatched(_clean_db):
    from ai_engine.persistence import staff_presence
    await db.execute(
        "INSERT INTO staff(id, role, name, password_hash) "
        "VALUES ('A', 'agent', 'A', '') ON CONFLICT(id) DO NOTHING"
    )
    await staff_presence.heartbeat("A", "online")
    cid = await conv_dao.create_conversation(user_type="c", subject_id="u")
    info = await create_ticket._ensure_human_pending(cid)
    assert info == {"no_one_online": False}


@pytest.mark.asyncio
async def test_ensure_human_pending_returns_no_off_hours_field(_clean_db):
    """旧的 off_hours / next_shift_start 字段必须删干净。"""
    cid = await conv_dao.create_conversation(user_type="c", subject_id="u")
    info = await create_ticket._ensure_human_pending(cid)
    assert "off_hours" not in info
    assert "next_shift_start" not in info
```

- [ ] **Step 2: 运行测试确认 FAIL**

Run: `cd server && uv run pytest tests/agent/test_create_ticket_dispatch.py -v`
Expected: FAIL（仍返回 off_hours/next_shift_start 老字段）

- [ ] **Step 3: 改 _ensure_human_pending**

把 `server/src/ai_engine/agent/tools/create_ticket.py:255-284` 整段替换为：

```python
async def _ensure_human_pending(conversation_id: int) -> dict[str, Any]:
    """当前 mode=ai 时切到 human_pending 并广播 mode_change，然后触发派单。
    返回 {no_one_online: bool} 让上层 AI 据此措辞（True 则提示"客服当前不在线"）。"""
    info: dict[str, Any] = {"no_one_online": False}
    try:
        from ai_engine.api.staff_conversations import publish_conversation_event
        from ai_engine.persistence import conversations as conv_dao
        from ai_engine.persistence.staff_metrics import refresh_human_pending
        from ai_engine.services.dispatch import dispatch_to_human_pending

        mode, _ = await conv_dao.get_mode(conversation_id)
        if mode == "ai":
            await conv_dao.set_mode(conversation_id, "human_pending")
            await refresh_human_pending()
            publish_conversation_event(
                conversation_id, {"type": "mode_change", "to": "human_pending"}
            )

        result = await dispatch_to_human_pending(conversation_id)
        info["no_one_online"] = bool(result.get("no_one_online"))
    except Exception:
        # 派单失败不阻断工单创建——工单已经入库，事项中心已推送
        pass
    return info
```

- [ ] **Step 4: 运行测试确认 PASS**

Run: `cd server && uv run pytest tests/agent/test_create_ticket_dispatch.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add server/src/ai_engine/agent/tools/create_ticket.py server/tests/agent/test_create_ticket_dispatch.py
git commit -m "refactor(create_ticket): _ensure_human_pending 改走 dispatch，删 off_hours/next_shift_start 字段"
```

---

### Task 7: user_events.request_human 改用 dispatch

**Files:**
- Modify: `server/src/ai_engine/api/user_events.py:14, 34-42, 57, 71`
- Test: `server/tests/api/c_side/test_user_events_dispatch.py`

- [ ] **Step 1: 写测试**

```python
# server/tests/api/c_side/test_user_events_dispatch.py
import pytest
from httpx import AsyncClient

from ai_engine.persistence import conversations as conv_dao
from ai_engine.persistence import db, staff_presence


@pytest.mark.asyncio
async def test_request_human_no_one_online(client: AsyncClient, _clean_db, _c_user_token):
    cid = await conv_dao.create_conversation(user_type="c", subject_id="u-rh")
    r = await client.post(
        f"/api/v1/conversations/{cid}/request-human",
        json={"reason": "test"},
        headers={"Authorization": f"Bearer {_c_user_token('u-rh')}"},
    )
    assert r.status_code == 200
    body = r.json()
    assert body["ok"] is True
    assert body["no_one_online"] is True
    assert "off_hours" not in body
    assert "next_shift_start" not in body


@pytest.mark.asyncio
async def test_request_human_dispatches_to_online_staff(
    client: AsyncClient, _clean_db, _c_user_token
):
    await db.execute(
        "INSERT INTO staff(id, role, name, password_hash) "
        "VALUES ('A', 'agent', 'A', '') ON CONFLICT(id) DO NOTHING"
    )
    await staff_presence.heartbeat("A", "online")
    cid = await conv_dao.create_conversation(user_type="c", subject_id="u-rh2")

    r = await client.post(
        f"/api/v1/conversations/{cid}/request-human",
        json={"reason": "x"},
        headers={"Authorization": f"Bearer {_c_user_token('u-rh2')}"},
    )
    body = r.json()
    assert body["no_one_online"] is False
    row = await db.fetch_one("SELECT assigned_staff_id FROM conversations WHERE id=:c", {"c": cid})
    assert row["assigned_staff_id"] == "A"
```

> 注意 fixture 名（`_c_user_token` / `client`）对照同目录其他测试调整。

- [ ] **Step 2: 运行测试确认 FAIL**

Run: `cd server && uv run pytest tests/api/c_side/test_user_events_dispatch.py -v`
Expected: FAIL（返回 off_hours / next_shift_start）

- [ ] **Step 3: 改 user_events.py**

修改 `server/src/ai_engine/api/user_events.py`：

```python
# line 14 删除：from ai_engine.persistence import shifts_query
# 改为新增 import：
from ai_engine.services.dispatch import dispatch_to_human_pending

# line 27-71 的 request_human 函数整体替换为
@router.post("/api/v1/conversations/{conv_id}/request-human")
async def request_human(conv_id: int, body: RequestHumanIn, request: Request) -> dict[str, Any]:
    user_type, subject_id = await resolve_identity(request)
    if user_type == USER_TYPE_GUEST:
        raise HTTPException(403, _t("handoff.guest_blocked", body.ui_locale))
    conv = await conv_dao.get_conversation(conv_id)
    if conv is None or conv["subject_id"] != subject_id or conv["user_type"] != user_type:
        raise HTTPException(403, "not your conversation")

    mode, _ = await conv_dao.get_mode(conv_id)
    if mode == "human_takeover":
        # 已有人接管，不再派单/不再 dispatch，但仍返回 no_one_online=False 给前端措辞
        return {"ok": True, "note": "already human-handled", "no_one_online": False}

    if mode == "human_pending":
        # 幂等：已是 pending，直接派一次（dispatch 内部对 assigned_staff_id IS NOT NULL 不覆盖）
        # 取最新 ticket 让 C 端能引用
        row = await db.fetch_one(
            "SELECT external_id FROM tickets WHERE conversation_id = :cid "
            "ORDER BY created_at DESC LIMIT 1",
            {"cid": conv_id},
        )
        existing_ticket = str(row["external_id"]) if row else None
        result = await dispatch_to_human_pending(conv_id)
        return {
            "ok": True,
            "status": "already_pending",
            "ticket_id": existing_ticket,
            "note": "already pending human takeover",
            "no_one_online": bool(result.get("no_one_online")),
        }

    await conv_dao.set_mode(conv_id, "human_pending")
    await refresh_human_pending()
    out = await create_ticket_run(
        subject_id=subject_id,
        user_type=user_type,
        conversation_id=conv_id,
        category="人工介入",
        summary=f"用户请求人工：{body.reason or '(no reason)'}",
        severity="p2",
        evidence={"conversation_id": conv_id, "reason": body.reason},
    )
    # create_ticket_run 内部 _ensure_human_pending 已经调过 dispatch；此处不重复派
    _publish(conv_id, {"type": "request_human", "ticket_id": out["external_ticket_id"]})
    return {
        "ok": True,
        "ticket_id": out["external_ticket_id"],
        "no_one_online": bool(out.get("no_one_online")),
    }
```

- [ ] **Step 4: 运行测试确认 PASS**

Run: `cd server && uv run pytest tests/api/c_side/test_user_events_dispatch.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add server/src/ai_engine/api/user_events.py server/tests/api/c_side/test_user_events_dispatch.py
git commit -m "refactor(user_events): request_human 删 shifts_query 改走 dispatch，响应字段切 no_one_online"
```

---

### Task 8: staff_conversations.take + list_for_staff 加 :me 过滤

**Files:**
- Modify: `server/src/ai_engine/persistence/conversations.py:281-305`（list_for_staff）
- Modify: `server/src/ai_engine/api/staff_conversations.py:154-160`（list_conversations 端点）
- Modify: `server/src/ai_engine/api/staff_conversations.py:174-191`（take 端点 WHERE）
- Test: `server/tests/api/test_staff_conversations_dispatch.py`

- [ ] **Step 1: 写测试**

```python
# server/tests/api/test_staff_conversations_dispatch.py
import pytest
from httpx import AsyncClient

from ai_engine.persistence import conversations as conv_dao
from ai_engine.persistence import db
from ai_engine.persistence.schema import now_str


@pytest.mark.asyncio
async def test_take_succeeds_when_assigned_to_me(
    client: AsyncClient, _clean_db, _agent_token
):
    sid = "A"
    cid = await conv_dao.create_conversation(user_type="c", subject_id="u")
    await db.execute(
        "UPDATE conversations SET mode='human_pending', assigned_staff_id=:s, assigned_at=:t "
        "WHERE id=:c",
        {"s": sid, "t": now_str(), "c": cid},
    )
    r = await client.post(
        f"/staff/api/v1/conversations/{cid}/take",
        headers={"Authorization": f"Bearer {_agent_token(sid)}"},
    )
    assert r.status_code == 200
    row = await db.fetch_one("SELECT mode FROM conversations WHERE id=:c", {"c": cid})
    assert row["mode"] == "human_takeover"


@pytest.mark.asyncio
async def test_take_fails_when_assigned_to_other(
    client: AsyncClient, _clean_db, _agent_token
):
    cid = await conv_dao.create_conversation(user_type="c", subject_id="u")
    await db.execute(
        "UPDATE conversations SET mode='human_pending', assigned_staff_id='B', assigned_at=:t "
        "WHERE id=:c",
        {"t": now_str(), "c": cid},
    )
    r = await client.post(
        f"/staff/api/v1/conversations/{cid}/take",
        headers={"Authorization": f"Bearer {_agent_token('A')}"},
    )
    assert r.status_code == 409


@pytest.mark.asyncio
async def test_list_human_pending_filters_other_staff_assignments(
    client: AsyncClient, _clean_db, _agent_token
):
    # 三个 pending：派给 A，派给 B，未派
    c_a = await conv_dao.create_conversation(user_type="c", subject_id="u1")
    c_b = await conv_dao.create_conversation(user_type="c", subject_id="u2")
    c_open = await conv_dao.create_conversation(user_type="c", subject_id="u3")
    await db.execute(
        "UPDATE conversations SET mode='human_pending', assigned_staff_id='A', assigned_at=:t "
        "WHERE id=:c",
        {"t": now_str(), "c": c_a},
    )
    await db.execute(
        "UPDATE conversations SET mode='human_pending', assigned_staff_id='B', assigned_at=:t "
        "WHERE id=:c",
        {"t": now_str(), "c": c_b},
    )
    await db.execute(
        "UPDATE conversations SET mode='human_pending' WHERE id=:c",
        {"c": c_open},
    )

    r = await client.get(
        "/staff/api/v1/conversations?status=human_pending",
        headers={"Authorization": f"Bearer {_agent_token('A')}"},
    )
    assert r.status_code == 200
    ids = sorted(int(row["id"]) for row in r.json())
    assert c_a in ids
    assert c_open in ids
    assert c_b not in ids
```

- [ ] **Step 2: 运行测试确认 FAIL**

Run: `cd server && uv run pytest tests/api/test_staff_conversations_dispatch.py -v`
Expected: FAIL（list 返回所有 pending，take 不过滤）

- [ ] **Step 3: 改 list_for_staff 加 :me 参数**

`server/src/ai_engine/persistence/conversations.py:281-305`：

```python
async def list_for_staff(
    filter_status: str = "all",
    risk_only: bool = False,
    me: str | None = None,
) -> list[dict[str, object]]:
    """客服工作台列表。

    - filter_status ∈ {human_pending, human_takeover, all}：默认 mode 过滤（risk_only=False）。
    - risk_only=True：忽略 mode，按"任一风险信号为真"筛选（含 mode='ai' 的答错会话）。
    - me：当 filter_status='human_pending' 时，只返回开放池或派给我的会话（隔离别人未到期的派单）。

    无论何种过滤，每行都附带风险标记字段：has_failed / has_out_of_scope /
    has_downvote / has_empty_tool（needs_review 为表自身列，随 c.* 返回）。
    """
    cols = f"c.*, {_RISK_SELECT}"
    if risk_only:
        return await db.fetch_all(
            f"SELECT {cols} FROM conversations c WHERE {_RISK_WHERE} "
            "ORDER BY c.id DESC LIMIT 100"
        )
    if filter_status == "all":
        return await db.fetch_all(
            f"SELECT {cols} FROM conversations c WHERE c.mode != 'ai' ORDER BY c.id DESC LIMIT 100"
        )
    if filter_status == "human_pending" and me:
        return await db.fetch_all(
            f"SELECT {cols} FROM conversations c WHERE c.mode='human_pending' "
            f"AND (c.assigned_staff_id IS NULL OR c.assigned_staff_id=:me) "
            f"ORDER BY c.id DESC LIMIT 100",
            {"me": me},
        )
    return await db.fetch_all(
        f"SELECT {cols} FROM conversations c WHERE c.mode=:m ORDER BY c.id DESC LIMIT 100",
        {"m": filter_status},
    )
```

- [ ] **Step 4: 改 list_conversations 端点透传 staff sub**

`server/src/ai_engine/api/staff_conversations.py:154-160`：

```python
@router.get("/staff/api/v1/conversations")
async def list_conversations(
    status: str = Query("human_pending"),
    risk_only: bool = Query(False),
    staff: dict[str, Any] = Depends(require_staff),
) -> list[dict[str, object]]:
    return await conv_dao.list_for_staff(
        status, risk_only=risk_only, me=str(staff["sub"])
    )
```

- [ ] **Step 5: 改 take WHERE 子句**

`server/src/ai_engine/api/staff_conversations.py:178-184`：

```python
cur = await conn.execute(
    text(
        "UPDATE conversations SET mode='human_takeover', assigned_staff_id=:sub, "
        "assigned_at=:now WHERE id=:id AND "
        "(assigned_staff_id IS NULL OR assigned_staff_id=:sub)"
    ),
    {"sub": staff["sub"], "now": now_str(), "id": conv_id},
)
```

- [ ] **Step 6: 运行测试确认 PASS**

Run: `cd server && uv run pytest tests/api/test_staff_conversations_dispatch.py -v`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add server/src/ai_engine/persistence/conversations.py server/src/ai_engine/api/staff_conversations.py server/tests/api/test_staff_conversations_dispatch.py
git commit -m "feat(staff_conversations): list/take 加 :me 过滤（隔离别人未到期的派单）"
```

---

## Phase 3 — 删 shifts 全套

### Task 9: 删后端 shifts 代码

**Files:**
- Delete: `server/src/ai_engine/persistence/admin_shifts.py`
- Delete: `server/src/ai_engine/persistence/shifts_query.py`
- Delete: `server/src/ai_engine/api/admin_shifts.py`
- Delete: `server/tests/persistence/test_shifts_query.py`
- Delete: `server/tests/persistence/test_admin_shifts_dao.py`
- Delete: `server/tests/api/admin/test_admin_shifts_api.py`
- Modify: `server/src/ai_engine/main.py:9, 66`（删 router import + include）
- Modify: `server/src/ai_engine/persistence/schema.py:215-225`（删 staff_shifts 表 + index）
- Modify: `server/src/ai_engine/persistence/staff_presence.py:34-56`（删 list_active 的 on_shift_only 分支）

- [ ] **Step 1: 删除文件**

```bash
rm server/src/ai_engine/persistence/admin_shifts.py
rm server/src/ai_engine/persistence/shifts_query.py
rm server/src/ai_engine/api/admin_shifts.py
rm server/tests/persistence/test_shifts_query.py
rm server/tests/persistence/test_admin_shifts_dao.py
rm server/tests/api/admin/test_admin_shifts_api.py
```

- [ ] **Step 2: 改 main.py 删 router 注册**

`server/src/ai_engine/main.py`：
- 删 line 9: `from ai_engine.api.admin_shifts import router as admin_shifts_router`
- 删 line 66: `app.include_router(admin_shifts_router)`

- [ ] **Step 3: 改 schema.py 删 staff_shifts 表 + index**

`server/src/ai_engine/persistence/schema.py:215-225` 整段删除（注释和 `staff_shifts = Table(...)` 加 `Index(...)`）。

- [ ] **Step 4: 改 staff_presence.py 删 on_shift_only 分支**

`server/src/ai_engine/persistence/staff_presence.py:34-56` 整段替换为：

```python
async def list_active(window_seconds: int = 300) -> list[dict[str, Any]]:
    """返回当前 status != offline 且 last_seen_at 在窗口内的客服。

    新模型下没有"在班"概念——所有 online 即可派单（再由 RBAC chat.dispatch 过滤）。
    """
    cutoff = (datetime.now(UTC) - timedelta(seconds=window_seconds)).strftime("%Y-%m-%d %H:%M:%S")
    return await db.fetch_all(
        "SELECT staff_id, status, last_seen_at FROM staff_presence "
        "WHERE status != 'offline' AND last_seen_at >= :cutoff ORDER BY staff_id",
        {"cutoff": cutoff},
    )
```

并修改 `api/staff_presence.py` 中如果有传 `on_shift_only=True` 的调用点（grep 确认）：

```bash
grep -n "on_shift_only" server/src/ai_engine/api/staff_presence.py
```

如果有，全部去掉这个参数。

- [ ] **Step 5: 跑全后端测试，看有无残留引用**

Run: `cd server && uv run pytest -x --ignore=tests/integration 2>&1 | head -50`
Expected: 全 PASS（如有遗漏 import shifts_query / admin_shifts 的地方，按报错修）

可能漏的：`tests/api/c_side/test_chat_human_mode.py` 之类可能用了 shifts 的，全 grep 一遍：

```bash
grep -rn "shifts_query\|admin_shifts\|staff_shifts" server/ 2>/dev/null
```

任何残留按上下文删/改。

- [ ] **Step 6: Commit**

```bash
git add -A server/
git commit -m "feat(shifts): 删 staff_shifts 全套（表/查询/API/admin/测试），改用 dispatch 模型"
```

---

### Task 10: Prompt v1.0.0 + v1.1.0 改 handoff 措辞分支

**Files:**
- Modify: `server/src/ai_engine/prompts/v1.0.0/reply_style.b.md`
- Modify: `server/src/ai_engine/prompts/v1.1.0/reply_style.b.md`

- [ ] **Step 1: 读 v1.0.0 现有内容**

Run: `cat server/src/ai_engine/prompts/v1.0.0/reply_style.b.md | head -40`

确认现有"班外转人工的措辞"小节用了 `off_hours` / `next_shift_start` 字段说明。

- [ ] **Step 2: v1.0.0 替换该小节**

打开 `server/src/ai_engine/prompts/v1.0.0/reply_style.b.md`。找到从 `**班外转人工的措辞**` 起到该小节结尾的整段（大概到下一个二级标题之前），替换为：

```markdown
**没有在线客服时的措辞（旧版"班外"）**：`create_ticket(category="人工介入")` 返回里有 `no_one_online: bool` 字段。

- `no_one_online=false`（有客服在线）：按上面默认话术 "已为您创建工单，工程师/客服会尽快联系您"。
- `no_one_online=true`（无客服在线）：**绝对不要说**"已为您接通"/"客服会尽快"这类暗示有人在线的话——会误导用户原地等。改用以下措辞按用户语言择一：

| 语言 | 无客服在线措辞 |
| zh | "客服当前不在线，已为您留单，上线后会主动联系您。" |
| en | "No agent is online right now. We've logged your request and will contact you when an agent comes online." |
| pt-BR | "Não há atendente online no momento. Registramos sua solicitação e entraremos em contato assim que houver disponibilidade." |
| id | "Saat ini tidak ada agen yang online. Permintaan Anda telah dicatat dan akan kami hubungi setelah agen tersedia." |
| ja | "現在オペレーターはオンラインではありません。お問い合わせは記録されており、オペレーターがオンラインになり次第ご連絡いたします。" |

注意：旧的 `off_hours` / `next_shift_start` 字段已删除——不要在工具返回里寻找这两个字段，它们不再存在。
```

- [ ] **Step 3: v1.1.0 同样替换**

打开 `server/src/ai_engine/prompts/v1.1.0/reply_style.b.md`，做和 v1.0.0 完全相同的替换（保持两版同步，按 [project_prompt-version-dual-edit](memory)）。

- [ ] **Step 4: 跑相关测试看 prompt 集成有无破坏**

Run: `cd server && uv run pytest tests/agent/ -k handoff -v`
Expected: PASS（若有 prompt 校验类的测试触及旧字段，更新它们）

- [ ] **Step 5: Commit**

```bash
git add server/src/ai_engine/prompts/
git commit -m "feat(prompts): handoff 措辞 v1.0.0/v1.1.0 同步从 off_hours/next_shift_start 改 no_one_online"
```

---

## Phase 4 — 前端

### Task 11: PresenceToggle 组件 + 测试

**Files:**
- Create: `web/src/components/PresenceToggle.tsx`
- Create: `web/src/api/staffPresence.ts`（若不存在）
- Create: `web/tests/components/PresenceToggle.test.tsx`

- [ ] **Step 1: 写 api 层**

新建 `web/src/api/staffPresence.ts`：

```typescript
import { staffFetch } from "./staffFetch";

export type PresenceStatus = "online" | "offline";

export async function postPresence(
  token: string,
  status: PresenceStatus,
): Promise<{ ok: boolean; released_count: number }> {
  const r = await staffFetch("/staff/api/v1/presence", {
    method: "POST",
    headers: {
      Authorization: `Bearer ${token}`,
      "Content-Type": "application/json",
    },
    body: JSON.stringify({ status }),
  });
  if (!r.ok) throw new Error(`presence ${status} failed: ${r.status}`);
  const body = await r.json();
  return { ok: !!body.ok, released_count: Number(body.released_count ?? 0) };
}
```

参考 `web/src/api/staffFetch.ts:21`（导出名）和现有 api 文件（如 `web/src/api/adminShifts.ts`，删除前可参考）的 token 透传模式。

- [ ] **Step 2: 写组件测试**

```typescript
// web/tests/components/PresenceToggle.test.tsx
import { render, screen, fireEvent, waitFor } from "@testing-library/react";
import { App as AntApp } from "antd";
import { describe, it, expect, vi, beforeEach } from "vitest";

import { PresenceToggle } from "../../src/components/PresenceToggle";
import * as api from "../../src/api/staffPresence";

const renderWith = (ui: React.ReactNode) =>
  render(<AntApp>{ui}</AntApp>);

describe("PresenceToggle", () => {
  beforeEach(() => vi.restoreAllMocks());

  it("登录默认 offline", () => {
    renderWith(<PresenceToggle token="t" />);
    expect(screen.getByRole("switch")).not.toBeChecked();
    expect(screen.getByText(/离线/)).toBeInTheDocument();
  });

  it("点开 → 调 postPresence(online) → 切换显示", async () => {
    vi.spyOn(api, "postPresence").mockResolvedValue({ ok: true, released_count: 0 });
    renderWith(<PresenceToggle token="t" />);
    fireEvent.click(screen.getByRole("switch"));
    await waitFor(() =>
      expect(api.postPresence).toHaveBeenCalledWith("t", "online"),
    );
    expect(screen.getByText(/在线/)).toBeInTheDocument();
  });

  it("从 online 关到 offline，released_count>0 时弹提示", async () => {
    vi.spyOn(api, "postPresence")
      .mockResolvedValueOnce({ ok: true, released_count: 0 })  // online first
      .mockResolvedValueOnce({ ok: true, released_count: 2 }); // offline second
    renderWith(<PresenceToggle token="t" />);
    fireEvent.click(screen.getByRole("switch"));
    await waitFor(() => expect(screen.getByText(/在线/)).toBeInTheDocument());
    fireEvent.click(screen.getByRole("switch"));
    await waitFor(() =>
      expect(screen.getByText(/已释放 2 个未接管会话给其他客服/)).toBeInTheDocument(),
    );
  });
});
```

- [ ] **Step 3: 运行测试确认 FAIL**

Run: `cd web && pnpm test PresenceToggle`
Expected: FAIL with `Cannot find module 'PresenceToggle'`

- [ ] **Step 4: 实现组件**

新建 `web/src/components/PresenceToggle.tsx`：

```typescript
import { App, Space, Switch, Typography } from "antd";
import { useEffect, useRef, useState } from "react";

import { postPresence } from "../api/staffPresence";

const HEARTBEAT_INTERVAL_MS = 5 * 60 * 1000; // 5 分钟

export function PresenceToggle({ token }: { token: string }) {
  const [online, setOnline] = useState(false);
  const [loading, setLoading] = useState(false);
  const heartbeatRef = useRef<ReturnType<typeof setInterval> | null>(null);
  const { message } = App.useApp();

  useEffect(() => {
    return () => {
      if (heartbeatRef.current) clearInterval(heartbeatRef.current);
    };
  }, []);

  async function toggle(checked: boolean) {
    setLoading(true);
    try {
      const r = await postPresence(token, checked ? "online" : "offline");
      setOnline(checked);
      if (checked) {
        if (heartbeatRef.current) clearInterval(heartbeatRef.current);
        heartbeatRef.current = setInterval(() => {
          postPresence(token, "online").catch(() => {});
        }, HEARTBEAT_INTERVAL_MS);
      } else {
        if (heartbeatRef.current) {
          clearInterval(heartbeatRef.current);
          heartbeatRef.current = null;
        }
        if (r.released_count > 0) {
          message.info(`已释放 ${r.released_count} 个未接管会话给其他客服`);
        }
      }
    } catch (e) {
      message.error(e instanceof Error ? e.message : "切换失败");
    } finally {
      setLoading(false);
    }
  }

  return (
    <Space>
      <Switch checked={online} loading={loading} onChange={toggle} />
      <Typography.Text type={online ? "success" : "secondary"}>
        {online ? "在线" : "离线"}
      </Typography.Text>
    </Space>
  );
}
```

- [ ] **Step 5: 运行测试确认 PASS**

Run: `cd web && pnpm test PresenceToggle`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add web/src/components/PresenceToggle.tsx web/src/api/staffPresence.ts web/tests/components/PresenceToggle.test.tsx
git commit -m "feat(presence): PresenceToggle 组件 + 心跳定时器 + 关时 released_count 提示"
```

---

### Task 12: 工作台 layout 挂上 PresenceToggle

**Files:**
- Modify: `web/src/components/app-shell/`（顶栏 layout，按实际文件名找）

- [ ] **Step 1: 找客服工作台 layout 文件**

Run: `ls web/src/components/app-shell/`，找类似 `app-shell.tsx` / `top-bar.tsx` / `nav-bar.tsx` 的顶栏文件。

可能命名：`AppShell.tsx` / `Header.tsx` / `TopBar.tsx`。

- [ ] **Step 2: 找到客服角色的顶栏渲染区域，挂 PresenceToggle**

打开找到的顶栏组件，在用户头像旁加：

```typescript
import { PresenceToggle } from "../PresenceToggle";

// 在顶栏 JSX 里、用户头像旁边添加（仅客服角色可见——这块按现有 app-shell 区分 role 的方式决定）：
{role !== "admin" && <PresenceToggle token={token} />}
```

具体位置看 app-shell 的角色分流：
- 若 app-shell 直接区分了 `admin` vs `staff`，挂在 staff 那侧
- 若不区分，挂顶栏右上角，且加个 `hasPermission("chat.dispatch")` 之类的过滤

- [ ] **Step 3: 跑 typecheck 确认无类型错**

Run: `cd web && pnpm typecheck`
Expected: 无错误

- [ ] **Step 4: 跑 dev 看顶栏（手动验证）**

Run: 让用户起 `cd web && pnpm dev`（注意 dev server 必须在主仓库或 worktree 一致的地方，见 [feedback_dev-server-worktree-mismatch](memory)）

用户用 agent 账号登录后台，观察顶栏出现"离线" toggle。

- [ ] **Step 5: Commit**

```bash
git add web/src/components/app-shell/
git commit -m "feat(app-shell): 客服顶栏挂 PresenceToggle"
```

---

### Task 13: 删前端 shifts 全套

**Files:**
- Delete: `web/src/routes/admin/ShiftsRoute.tsx`
- Delete: `web/src/api/adminShifts.ts`
- Delete: `web/tests/...shifts*` 相关测试（grep 找）
- Modify: `web/src/App.tsx:8, 50`（删 import + Route）
- Modify: `web/src/components/app-shell/nav-config.ts:49`（删 shifts 菜单项）
- Modify: `web/src/components/app-shell/perm-map.ts:8`（删 admin.shifts 映射）
- Modify: `web/src/routes/admin/RbacRoute.tsx:20`（删 "admin.shifts": "排班" 标签）

- [ ] **Step 1: grep 所有 shifts 引用**

Run: `grep -rn "shifts\|Shifts" web/src web/tests 2>/dev/null | grep -v node_modules`

记录所有引用点。

- [ ] **Step 2: 删主要文件**

```bash
rm web/src/routes/admin/ShiftsRoute.tsx
rm web/src/api/adminShifts.ts
find web/tests -name "*shifts*" -o -name "*Shifts*" | xargs -I {} rm {}
```

- [ ] **Step 3: 改 App.tsx**

`web/src/App.tsx`：
- 删 line 8: `import { ShiftsRoute } from "./routes/admin/ShiftsRoute";`
- 删 line 50: `<Route path="/admin/shifts" element={<ShiftsRoute />} />`

- [ ] **Step 4: 改 nav-config.ts**

`web/src/components/app-shell/nav-config.ts:49` 整行删除（`{ to: "/admin/shifts", ... }`）

如果导入了 `CalendarClock` 图标但仅 shifts 用，把 import 也删了避免 `unused import` 报错。

- [ ] **Step 5: 改 perm-map.ts**

`web/src/components/app-shell/perm-map.ts:8` 删 `"/admin/shifts": "admin.shifts",`

- [ ] **Step 6: 改 RbacRoute.tsx**

`web/src/routes/admin/RbacRoute.tsx:20` 删 `"admin.shifts": "排班",`

如果同一对象里增加 `"chat.dispatch": "派单（接客户对话）"` 一项，让权限矩阵 UI 显示中文标签。

- [ ] **Step 7: typecheck + 跑测试**

```bash
cd web && pnpm typecheck && pnpm test
```
Expected: 无 type 错；测试全 PASS

- [ ] **Step 8: build 确认 dist 重新生成**

Run: `cd web && pnpm build`
Expected: 成功；产生新的 dist hash

- [ ] **Step 9: Commit**

```bash
git add -A web/
git commit -m "feat(shifts): 删前端排班路由/API/菜单/权限映射，权限矩阵加 chat.dispatch 标签"
```

---

## Phase 5 — DB migration + 端到端验证

### Task 14: DB migration（DROP staff_shifts + away→offline）

**Files:**
- Modify or Create: `server/src/ai_engine/persistence/maintenance.py`（已有 maintenance 模块）或独立 migration 脚本

- [ ] **Step 1: 确认现有 migration 风格**

Run: `cat server/src/ai_engine/persistence/maintenance.py | head -40`

若项目有"启动时自动跑 migration"机制，加在那里；若无，写独立脚本 `server/scripts/migrate_2026_06_04_drop_shifts.py`。

- [ ] **Step 2: 写 migration 脚本**

新建 `server/scripts/migrate_2026_06_04_drop_shifts.py`：

```python
"""2026-06-04 migration: drop staff_shifts table + collapse away→offline.

Run: cd server && uv run python scripts/migrate_2026_06_04_drop_shifts.py
"""

import asyncio

from ai_engine.persistence import db


async def main():
    # db.execute / execute_rowcount 内部自管 engine + 事务，无需外层 context。
    try:
        await db.execute("DROP TABLE IF EXISTS staff_shifts")
        print("dropped staff_shifts table")
    except Exception as e:
        print(f"drop staff_shifts failed: {e}")

    affected = await db.execute_rowcount(
        "UPDATE staff_presence SET status='offline' WHERE status='away'"
    )
    print(f"collapsed away→offline: {affected} rows")


if __name__ == "__main__":
    asyncio.run(main())
```

> 用的是 `db.execute` / `db.execute_rowcount`（顶层 helper），见 `server/src/ai_engine/persistence/db.py:65-74`。

- [ ] **Step 3: 本地 SQLite 跑一遍**

Run: `cd server && uv run python scripts/migrate_2026_06_04_drop_shifts.py`
Expected: 输出 "dropped staff_shifts table"

- [ ] **Step 4: 验证**

```bash
sqlite3 server/ai_engine.db ".tables" | tr ' ' '\n' | grep shift
```
Expected: 无输出（表已删）

- [ ] **Step 5: Commit**

```bash
git add server/scripts/migrate_2026_06_04_drop_shifts.py
git commit -m "chore(migration): drop staff_shifts table + collapse away→offline"
```

---

### Task 15: docker rebuild + 手动 E2E 验证

**Files:** 无新文件，纯运维 + 手动测试

- [ ] **Step 1: docker rebuild 后端**

Run: `docker compose up -d --build api`（按 [project_backend-docker-rebuild](memory)，restart 不够）
Expected: api 容器重启成功，日志无报错

- [ ] **Step 2: 容器内跑 migration（生产 PG）**

如果 prod 用 docker postgres：

```bash
docker compose exec api uv run python scripts/migrate_2026_06_04_drop_shifts.py
```

- [ ] **Step 3: 手动 E2E — 多客服派单均衡**

场景：起两个客服账号（agent-1 / agent-2），都打开开关 → C 端发起 3 次转人工 → 观察 3 次都派给不同人（或当前在聊数最少的）

```text
1. 浏览器窗口 1：登录 agent-1，点工作台顶栏开关切到"在线"
2. 浏览器窗口 2：登录 agent-2，点开关切到"在线"
3. 浏览器窗口 3：登录 C 端 H5（不同用户身份），AI 对话后点"转人工"
4. 观察：窗口 1 的工作台是否收到 conversation_assigned SSE / 列表出现该会话
5. agent-1 点"接管" → 会话进 human_takeover
6. 再起浏览器窗口 4 / 5：另两次转人工
7. 观察：第 4 次应派给 agent-2（agent-1 已在聊 1 个），第 5 次应再派给 agent-2（agent-1=1, agent-2=1, 同载时 tiebreaker 最近活跃）
```

- [ ] **Step 4: 手动 E2E — 60s 超时回退**

```text
1. agent-1 / agent-2 都 online
2. C 端转人工 → 派给 agent-1
3. agent-1 故意 60 秒不点 take（等）
4. 观察：60-70 秒后该会话出现在 agent-2 工作台的"未接管"池
5. agent-2 可以 take 成功
```

- [ ] **Step 5: 手动 E2E — 关开关释放未接管**

```text
1. agent-1 在线，被派 1 个会话（未 take）
2. agent-1 在工作台点开关切到"离线"
3. 观察：弹 message.info "已释放 1 个未接管会话给其他客服"
4. agent-2 立刻在自己工作台看到该会话进入"未接管"池
```

- [ ] **Step 6: 手动 E2E — 班外（全员 offline）**

```text
1. agent-1 / agent-2 都关开关
2. C 端转人工
3. 观察：AI 用"客服当前不在线，已为您留单..."措辞（5 种语言之一，按用户语言切换验证）
4. 观察：admin/admin 后台"会话"页能看到该 human_pending 会话（assigned_staff_id=NULL）
```

- [ ] **Step 7: 手动 E2E — 已接管不释放**

```text
1. agent-1 已 take 一个会话（mode=human_takeover）
2. agent-1 关开关
3. 观察：该已接管会话**不释放**，agent-1 仍能在工作台回复
4. agent-1 自己点"已解决"才结束该会话
```

- [ ] **Step 8: 跨子项目同步检查**

按全局指令 §五 第 17 条：

回答："其他端是否需要同步？检查结果是：
- backend 改完，本仓库 server 已含完整改动
- web（客服工作台）改完，本仓库 web 已含
- C端 APP / B端 OpenAPI / 管理后台：本次仅改 cs-engine 内部模型（presence/shifts/dispatch），未动外部 API 契约，**无需同步**
- 事项中心：未动推送契约，无需同步"

- [ ] **Step 9: 提交最终 PR / 合并主干（按用户指令）**

不主动 push 也不主动 merge，等用户拍。

---

## Self-Review 备忘（执行 plan 前已校对）

- spec §3 表 / §4 API / §5 算法 / §6 关开关 / §7 i18n＆prompt / §8 RBAC / §9 删除清单 / §10 测试 / §11 上线步骤 全部有对应 Task。
- 类型/方法签名一致性：`release_pending_assignments(staff_id) -> list[int]`；`dispatch_to_human_pending(conv_id) -> dict`；`_ensure_human_pending(conv_id) -> dict {no_one_online: bool}`；`postPresence(token, status) -> {ok, released_count}`。
- 无 placeholder（所有 code 步骤含完整代码）。
- 旧字段 `off_hours` / `next_shift_start` 在 Task 6/7/10 三处都明确删除；Task 6 测试覆盖了"删干净"。
